<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.david.lightandroidproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".navigation.DemoStandardActivity" />
        <activity android:name=".navigation.DemoDrawerActivity" />
        <activity android:name=".navigation.DemoTabbedActivity" />
        <activity android:name=".navigation.DemoDrawerMenuActivity" />
        <activity android:name=".navigation.DemoRecyclerActivity" />
        <activity android:name=".DemoNetworkActivity" />
        <activity
            android:name=".CameraActivity"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" />
    </application>

</manifest>
